<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Food">

    <select id="getFoodList" resultType="kr.co.siggy.server.common.CamelCaseMap">
		SELECT
			SEQ
			, NAME
			, DESCRIPT
			, LAT
			, LNG
			, CATEGORY
			, TYPE
			, NAVER_MAP_URL
		FROM 
			FOOD_LOCATION
		WHERE
			CATEGORY = #{category}
    </select>

    <select id="getFoodLikeSeqList" resultType="Integer">
		SELECT
			MAP_SEQ
		FROM 
			USER_FAVORITE
		WHERE 
			USER_ID = #{id}
		AND 
			CATEGORY = 'food'
    </select>

    
    <select id="getPlaceList" resultType="kr.co.siggy.server.common.CamelCaseMap">
		SELECT
			SEQ
			, NAME
			, DESCRIPT
		FROM 
			PLACE_LOCATION
    </select>
    
       <select id="getPlaceLikeSeqList" resultType="Integer">
		SELECT
			MAP_SEQ
		FROM 
			USER_FAVORITE
		WHERE 
			USER_ID = #{id}
		AND 
			CATEGORY = 'place'
    </select>



</mapper>
